---
# default-deny-all for agentic-observability namespace (ingress + egress)
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: default-deny-all
  namespace: agentic-observability
  labels:
    app.kubernetes.io/part-of: nvidia-ida
    app.kubernetes.io/component: networkpolicies
spec:
  podSelector: {}
  policyTypes:
    - Ingress
    - Egress

---
# Allow ingress from openshift-monitoring (Prometheus scrape for custom metrics
# and ServiceMonitor targets defined in this namespace).
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: allow-ingress-from-openshift-monitoring
  namespace: agentic-observability
  labels:
    app.kubernetes.io/part-of: nvidia-ida
    app.kubernetes.io/component: networkpolicies
spec:
  podSelector: {}
  policyTypes:
    - Ingress
  ingress:
    - from:
        - namespaceSelector:
            matchLabels:
              kubernetes.io/metadata.name: openshift-monitoring
      ports:
        - port: 9090
          protocol: TCP   # alertmanager / otel collector metrics
        - port: 8888
          protocol: TCP   # otel collector telemetry

---
# Allow egress to external Loki push endpoint (172.16.2.252:3100) and
# Grafana (172.16.2.252:3000) on the management LAN.
# LOKI_PUSH_URL = http://172.16.2.252:3100
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: allow-egress-loki-grafana
  namespace: agentic-observability
  labels:
    app.kubernetes.io/part-of: nvidia-ida
    app.kubernetes.io/component: networkpolicies
spec:
  podSelector: {}
  policyTypes:
    - Egress
  egress:
    - to:
        - ipBlock:
            cidr: 172.16.2.252/32
      ports:
        - port: 3100
          protocol: TCP   # Loki push (LOKI_PUSH_URL)
        - port: 3000
          protocol: TCP   # Grafana HTTP

---
# Allow egress to openshift-dns
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: allow-egress-dns
  namespace: agentic-observability
  labels:
    app.kubernetes.io/part-of: nvidia-ida
    app.kubernetes.io/component: networkpolicies
spec:
  podSelector: {}
  policyTypes:
    - Egress
  egress:
    - to:
        - namespaceSelector:
            matchLabels:
              kubernetes.io/metadata.name: openshift-dns
      ports:
        - port: 5353
          protocol: UDP
        - port: 5353
          protocol: TCP
        - port: 53
          protocol: UDP
        - port: 53
          protocol: TCP

---
# Allow egress to the Kubernetes API server.
# OVN-Kubernetes enforces NetworkPolicy after service DNAT: traffic to the
# kubernetes.default ClusterIP (172.30.0.1:443) reaches the host at :6443,
# so both forms must be allowed. Controllers/operators in this namespace
# (and webhook/injector clients) fail without this.
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: allow-egress-apiserver
  namespace: agentic-observability
  labels:
    app.kubernetes.io/part-of: nvidia-ida
    app.kubernetes.io/component: networkpolicies
spec:
  podSelector: {}
  policyTypes:
    - Egress
  egress:
    - to:
        - ipBlock:
            cidr: 172.30.0.1/32
      ports:
        - port: 443
          protocol: TCP
    - to:
        - ipBlock:
            cidr: 0.0.0.0/0
      ports:
        - port: 6443
          protocol: TCP
