# Patch: set Gateway listener hostname for anaeem cluster
# apps domain: apps.anaeem.na-launch.com
apiVersion: gateway.networking.k8s.io/v1
kind: Gateway
metadata:
  name: mcp-gateway
  namespace: mcp-gateway
spec:
  listeners:
    - name: http
      protocol: HTTP
      port: 8080
      hostname: "mcp-gateway.apps.anaeem.na-launch.com"
      allowedRoutes:
        namespaces:
          from: Same
