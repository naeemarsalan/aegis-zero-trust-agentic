apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# openshift-operators namespace + its global-operators OperatorGroup already exist
# cluster-side, so only the Subscription is managed here.
resources:
  - subscription.yaml
