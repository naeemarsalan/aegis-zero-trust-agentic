apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../../base

# No anaeem-specific patches needed for UWM — base values are appropriate for SNO.
# Add patches here if retention or resource limits need tuning per environment.
