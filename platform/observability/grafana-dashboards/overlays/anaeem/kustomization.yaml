apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../../base

# No anaeem-specific patches needed for dashboards.
# Dashboard JSON embeds the cluster=anaeem constant as a template variable.
# If the Loki/Prometheus datasource UIDs differ in your Grafana instance,
# update the __inputs[].name values in the dashboard JSON before importing.
