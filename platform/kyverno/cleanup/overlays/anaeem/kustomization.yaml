apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../../base

# anaeem-specific patches.
# Currently the base is already correct for anaeem (Kyverno 1.16+, ClusterCleanupPolicy available).
