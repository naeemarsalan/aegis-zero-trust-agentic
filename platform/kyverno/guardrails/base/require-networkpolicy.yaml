apiVersion: kyverno.io/v1
kind: ClusterPolicy
metadata:
  name: require-networkpolicy
  labels:
    app.kubernetes.io/component: kyverno-guardrail
    app.kubernetes.io/part-of: nvidia-ida
  annotations:
    policies.kyverno.io/title: Require and generate default-deny NetworkPolicy
    policies.kyverno.io/description: >
      Every platform namespace must have a default-deny NetworkPolicy.
      The generate rule automatically creates a default-deny-all policy when a
      new namespace matching our label selector is created, providing a
      zero-trust network baseline.  The validate rule ensures no one has
      accidentally deleted the default-deny policy from an existing namespace.
      validationFailureAction is Audit for PoC.
    # NOTE: flip validationFailureAction to Enforce before production.
spec:
  validationFailureAction: Audit
  background: true
  rules:
    # Auto-generate a default-deny NetworkPolicy when a new platform namespace appears.
    - name: generate-default-deny-networkpolicy
      match:
        any:
          - resources:
              kinds: ["Namespace"]
              selector:
                matchLabels:
                  app.kubernetes.io/part-of: nvidia-ida
      # Exclude OpenShift / Kubernetes system + operator namespaces. A BARE
      # default-deny (no allow-egress companions) cuts a namespace's pods off
      # from the apiserver ClusterIP + DNS; for operator/system namespaces that
      # lack curated allow rules this degrades cluster operators and crashloops
      # their controllers. This is exactly the 2026-06-16 outage: a default-deny
      # was generated into openshift-sandboxed-containers-operator (and infra
      # namespaces) which then lost the apiserver. Those namespaces must never
      # receive the blunt auto-generated baseline; their NetworkPolicies are
      # curated separately. synchronize:true will prune the previously generated
      # default-deny from any namespace newly matched by this exclude.
      exclude:
        any:
          - resources:
              kinds: ["Namespace"]
              names:
                - "openshift-*"
                - "kube-*"
                - "default"
      generate:
        apiVersion: networking.k8s.io/v1
        kind: NetworkPolicy
        name: default-deny
        namespace: "{{request.object.metadata.name}}"
        synchronize: true
        data:
          metadata:
            labels:
              app.kubernetes.io/managed-by: kyverno
              app.kubernetes.io/part-of: nvidia-ida
          spec:
            podSelector: {}
            policyTypes:
              - Ingress
              - Egress

    # Validate that a default-deny NetworkPolicy exists in platform namespaces.
    - name: check-default-deny-networkpolicy-exists
      match:
        any:
          - resources:
              kinds: ["Namespace"]
              namespaces:
                - zero-trust-workload-identity-manager
                - keycloak
                - vault
                - mcp-gateway
                - kyverno
                - agentic-mcp
                - agent-sandbox
                - agentic-observability
      validate:
        message: >
          Namespace must have a NetworkPolicy named 'default-deny' with
          podSelector: {} and policyTypes [Ingress, Egress].
        deny:
          conditions:
            all:
              - key: "{{ networkpolicies_count }}"
                operator: Equals
                value: 0
        foreach:
          - list: "request.object.metadata.name"
            context:
              - name: networkpolicies_count
                apiCall:
                  urlPath: "/apis/networking.k8s.io/v1/namespaces/{{element}}/networkpolicies"
                  jmesPath: "items[?metadata.name=='default-deny'] | length(@)"
