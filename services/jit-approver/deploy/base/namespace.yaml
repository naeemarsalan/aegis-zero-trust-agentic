# jit-approver runs in ns mcp-gateway alongside agentgateway and ext-proc-delegation.
# Namespace is NOT created here — it is owned by the agentgateway component.
# This file is a reference marker only.
