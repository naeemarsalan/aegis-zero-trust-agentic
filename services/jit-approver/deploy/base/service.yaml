apiVersion: v1
kind: Service
metadata:
  name: jit-approver
  namespace: mcp-gateway
  labels:
    app: jit-approver
spec:
  selector:
    app: jit-approver
  ports:
    - name: http
      port: 8080
      targetPort: 8080
      protocol: TCP
  type: ClusterIP
