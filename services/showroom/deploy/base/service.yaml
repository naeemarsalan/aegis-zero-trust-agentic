apiVersion: v1
kind: Service
metadata:
  name: showroom
  namespace: rhoai-agentic
  labels:
    app: showroom
spec:
  selector:
    app: showroom
  ports:
    - name: http
      port: 8080
      targetPort: 8080
      protocol: TCP
  type: ClusterIP
