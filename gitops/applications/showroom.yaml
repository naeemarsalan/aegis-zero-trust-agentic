apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: nvidia-ida-showroom
  namespace: openshift-gitops
  annotations:
    argocd.argoproj.io/sync-wave: "5"
spec:
  project: nvidia-ida

  source:
    repoURL: https://git.arsalan.io/anaeem/nvidia-ida.git
    targetRevision: main
    path: services/showroom/deploy/overlays/anaeem

  destination:
    server: https://api.anaeem.na-launch.com:6443
    namespace: showroom

  syncPolicy:
    automated:
      prune: true
      selfHeal: false
    syncOptions:
      - CreateNamespace=true
      - ServerSideApply=true
