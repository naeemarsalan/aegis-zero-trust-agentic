# Secrets and local environment — never commit
.env
*.env
!.env.example
!environment/.env.example
*kubeconfig*
*.pem
*.key
*.crt
# Claude Code local MCP config — contains local credentials
.mcp.json

# Rendered/generated output
rendered/
dist/
bin/

# Go
vendor/
*.test

# Editor/tooling
.idea/
.vscode/
*.swp
__pycache__/
*.pyc

# kustomize --enable-helm chart pull cache (repo-server fetches charts itself)
charts/
node_modules/
dist-dynamic/
dist-scalprum/

# Cluster secrets (Vault unseal/root) — never commit
environment/.env.ocp-dev
environment/.env*
