apiVersion: jit.anaeem.na-launch.com/v1alpha1
kind: JITGrant
metadata:
  name: jit-d863f4df-de7a-4751-91ea-447f43e05945
  creationTimestamp: '2026-06-21T13:38:39.986177+00:00'
spec:
  sessionId: d863f4df-de7a-4751-91ea-447f43e05945
  requesterSub: agent:openshell-sandbox-c0480a
  agentSpiffeId: spiffe://anaeem.na-launch.com/ns/openshell/sa/openshell-sandbox
  justification: "JitLoop round 2 \u2014 approve the echo dangerous tool on echo-mcp\
    \ via kagenti jit-gate; firewall/networkpolicy scope maps to a non-empty tool_scope\
    \ capability."
  requestedScope:
    namespace: agentic-mcp
    durationMinutes: 15
    rules:
    - verbs:
      - create
      resources:
      - networkpolicies
  vaultRoleCaps:
    role: jit-<sessionId> (ephemeral, created per approval)
    allowedNamespaces:
    - agentic-mcp
    ttlMax: 15m
    permissionsGranted: single-namespace SA token via kubernetes/creds/jit-<sessionId>;
      generated_role_rules == the reviewed verbs/resources above
    credentialDelivery: "stored in Vault KV secret/data/jit/<sessionId>; agent reads\
      \ via injector template \u2014 never returned over HTTP"
    cleanupBackstop: the expiry reaper MUST delete kubernetes/roles/jit-<sessionId>
      in addition to revoking the lease and the KV entry
